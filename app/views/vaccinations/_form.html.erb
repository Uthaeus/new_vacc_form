
<div class="row">
  <div class="col-sm-5">
    <%= form_with(model: vaccination, local: true) do |form| %>
      <% if vaccination.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(vaccination.errors.count, "error") %> prohibited this vaccination from being saved:</h2>

          <ul>
          <% vaccination.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :title %>
        <%= form.text_field :title, id: :vaccination_title, class: 'form-control' %>
      </div>

      <div class="form-group mid-marg">
        <%= form.submit 'Create Vaccination', class: 'btn btn-outline-success btn-block' %>
      </div>
    <% end %>
  </div>
</div>
